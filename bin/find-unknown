#!/bin/bash

addr=$1

last=$(get-addr-file "$addr" || echo code/008000)
while next=$(get-next-file "$addr"); do
	line=($(tail -1 $last))
	a=${line[0]}
	b=${line[1]}
	addr=$(rax2 0x$a+$((${#b}/2)) | xargs printf '%06x\n')
	if [ $addr != ${next#*/} ]; then
		echo $addr
		break
	fi
	last=$next
done
