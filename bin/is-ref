#!/bin/bash

addr=$1

f=$(get-addr-file "$addr") &&
[ "$f" = "${f#unref}" ] &&
tail -1 "$f" | {
	read a b rest
	nextaddr=$(rax2 0x$a+$((${#b}/2)) | xargs printf '%06x')
	[[ "$addr" < "$nextaddr" ]]
}
